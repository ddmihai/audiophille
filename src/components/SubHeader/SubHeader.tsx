


const SubHeader = ({ title }: { title: string }) => {
    return (
        <section className="bg-primary-black">
            <h1 className="text-white mx-auto max-w-fit py-16 text-4xl">{title}</h1>
        </section>
    )
}

export default SubHeader
